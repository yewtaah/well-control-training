import { AdminDashboard } from "./AdminDashboard";

export const metadata = {
  title: "Analytics | Well Command WC Training",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
